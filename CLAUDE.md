# Working instructions for Claude

- After making code changes in a session, commit and push to `origin main` at the end of the turn without asking first — the user has explicitly asked for this so changes go live without an extra prompt each time. Still use a clear, honest commit message describing what changed and why. Skip the commit if there's nothing to commit (no staged changes).
